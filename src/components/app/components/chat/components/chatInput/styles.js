const styles = ({ palette }) => ({
  container: {
    padding: 15,
  },
  inputWrapper: {
    width: '70%',
    borderRadius: 15,
    fontSize: 12,
    backgroundColor: palette.common.white,
  },
  input: {
    padding: 6,
  },
});

export default styles;

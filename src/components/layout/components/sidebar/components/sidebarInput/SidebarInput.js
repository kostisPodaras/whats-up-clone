import React from 'react';
import withStyles from '@material-ui/core/styles/withStyles';

import styles from './styles';

// type Props = {
//   classes: Object,
// };

const SidebarInput = () => {
  return <div>search</div>;
};

export default withStyles(styles)(SidebarInput);
